# AGENT.md

本文件是本项目交给 AI agent、自动化工具和新开发者时的工作契约。开始修改前请阅读，并以仓库当前代码为准；如果本文件与用户最新明确要求冲突，以用户要求为最高优先级。

## 1. 项目概况

- 项目：英语衡水体字帖生成器
- 仓库：https://github.com/SamuelChen-CN/hengshui-handwriting-generator
- 当前产品版本：`v1.0.0`
- 默认分支：`main`
- 形态：完全本地运行的单页面 Web 应用
- 目标：将英语作文排版为可预览、可打印的 A4 英语衡水体字帖
- 隐私：作文内容不上传服务器，不依赖账号、数据库或后端 API

## 2. 技术栈与运行环境

产品使用 HTML5、CSS3、原生 JavaScript、浏览器字体加载与打印能力，以及 `fonts/hengshui.ttf`。PDF 当前通过 `window.print()` 由浏览器保存。

开发环境为 Windows PowerShell、Node.js `>=18` 和 npm。Playwright 是开发依赖，用于后续浏览器自动化和视觉回归；浏览器运行文件不一定已安装。

项目无需启动开发服务器即可使用，直接打开 `index.html` 是当前支持的主要方式。若浏览器限制本地文件行为，可使用任意静态文件服务器预览。

## 3. 当前产品行为

- A4 纵向页面，CSS 固定页边距。
- 字体固定 `28px`，每行高度固定 `9mm`。
- 横线作为英语四线三格中的第三条线；主体字母在线上方，`g`、`y` 等下伸部分可落在线下。
- 横线为黑色，并在打印时保留。
- `抄写`：正常黑色示范文字。
- `临摹`：每条示范文字后增加一条相邻空白横线。
- `描红`：正常字体，黑色 30% 透明度。
- 输入换行和文末空行会保留为对应的空白横线。
- 英文按单词换行，避免拆分单词；长内容按 A4 可用高度分页。
- 顶部操作栏固定显示项目名、字体状态和打印按钮。
- 预览和打印共用同一套 DOM 与 CSS，必须尽量保持所见即所得。

## 4. 总体目标

持续将项目发展为可靠的本地英语字帖排版工具：排版稳定、打印一致、操作简单、隐私优先，并让不同 AI agent 能通过本文件、`README.md` 和 `TODO.md` 快速恢复上下文。

## 5. 目录结构与关键文件

```text
.
├── index.html              # 页面结构、顶部栏、输入区、模式选项
├── style.css               # UI、A4 页面、9mm 横线、字体和打印样式
├── app.js                  # 状态、换行、模式转换、分页和渲染
├── fonts/hengshui.ttf      # 核心字体资源
├── fonts/README.txt        # 字体放置说明
├── assets/                 # 图片、图标等预留资源
├── package.json            # 项目元信息和开发检查命令
├── package-lock.json       # npm 锁定文件
├── README.md               # 面向用户的使用说明
├── AGENT.md                # agent/开发者工作契约
├── TODO.md                 # 短期版本规划
├── CHANGELOG.md            # 版本变更记录
├── .gitignore              # Git 忽略规则
└── .gitattributes          # 文本换行和字体二进制属性
```

## 6. 核心数据流

```text
输入事件 → 等待 document.fonts.ready → 读取 currentStyle()
→ 创建临时 A4 页面并取得宽高 → makeWrappedLines() 按单词换行
→ applyMode() 处理模式 → 按 9mm 行高分页 → 创建 DOM
→ 浏览器预览 / window.print()
```

修改排版时应优先沿用这条数据流，不要为打印另写一套排版算法。

## 7. 编码与设计规范

### JavaScript

- 使用原生 DOM API，不引入框架，除非用户明确批准技术路线变化。
- 用户输入使用 `textContent`，禁止拼接进 `innerHTML`。
- 统一把 `\r\n`、`\r` 规范化为 `\n`，谨慎处理文末换行。
- 英文按单词换行，不默认使用 `word-break: break-all`。
- 新设置集中放入 `currentStyle()`；新模式使用明确的模式值和独立处理函数。
- 字体加载和渲染需考虑异步竞态，避免旧结果覆盖新输入。

### CSS

- 页面物理尺寸使用 `mm`，UI 尺寸可使用 `px`。
- 9mm 行高是核心约束，不能因字体大小变化而随意改变。
- 预览和打印共享 `.page`、`.page-content`、`.practice-line` 结构。
- 打印规则集中在 `@media print` 和 `@page`。
- 文字透明度不能影响横线颜色；文字与辅助线应能独立控制。
- 新增视觉效果前，验证 Chromium/Edge 打印支持并准备降级方案。

### 交互与安全

- 表单控件保留明确的 `label` 或 `legend`。
- 使用真实 `<button>`。
- 移动端不能遮挡输入框和预览区。
- 不向外部 API、云端服务或第三方分析服务发送作文内容。

## 8. 常用命令

```powershell
npm install
npm run check
git status
git log --oneline --decorate -5
```

当前没有正式的 `dev` 或 `build` 命令，也没有后端启动命令。不要无必要引入打包器、框架或服务端。

## 9. 验证要求

每次修改至少执行 `npm run check`。涉及排版时还要在浏览器中测试：字体加载、短中长作文、单次/连续/文末换行、三种模式、特殊英文标点、分页和打印预览。仅语法检查通过不能作为排版改动的完成标准。

## 10. Git 与写作规范

- 默认分支 `main`，不提交 `node_modules/`、日志或临时文件。
- 推荐提交格式：`feat:`、`fix:`、`docs:`、`refactor:`、`chore:`。
- 提交前检查 `git diff` 和 `git status`。
- 不使用 `git reset --hard` 或 `git checkout --` 覆盖用户改动，除非用户明确要求。
- 正式版本使用语义化版本标签，如 `v1.0.0`。
- 仅文档、交接规则或开发工具变更通常不升级产品版本；用户可见功能变化再评估版本。
- `README.md` 面向用户，不写 AI 内部协作过程；`AGENT.md` 面向 agent/开发者；`TODO.md` 记录可执行路线；重要行为变化同步 `CHANGELOG.md`。

## 11. 字体、隐私和版权硬约束

- 不得上传作文内容或默认接入云端处理。
- 不得擅自替换、压缩或重新分发 `fonts/hengshui.ttf`。
- 公开仓库继续保留字体前，必须确认其公开分发许可；无法确认时移除字体并保留放置说明。
- 不为了生成 PDF 默认引入降低文字清晰度的截图式方案。
- AI 改作文、语法检查、OCR、账户、云端保存等功能必须得到用户明确授权后再做。
